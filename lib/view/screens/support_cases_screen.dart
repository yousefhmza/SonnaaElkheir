import 'package:flutter/material.dart';

class SupportCasesScreen extends StatelessWidget {
  const SupportCasesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Support Cases Screen"),
      ),
    );
  }
}
