import 'package:flutter/material.dart';

class CalculateZakahScreen extends StatelessWidget {
  const CalculateZakahScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("حساب الزكاة"),
      ),
      body: Center(),
    );
  }
}
